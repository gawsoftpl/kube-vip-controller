apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-vip-controller.fullname" . }}-test-connection"
  labels:
    {{- include "kube-vip-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-vip-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
