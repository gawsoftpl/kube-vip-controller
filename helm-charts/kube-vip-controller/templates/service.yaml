apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-vip-controller.fullname" . }}
  labels:
    {{- include "kube-vip-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-vip-controller.selectorLabels" . | nindent 4 }}
